// Demo file - bad formatting, no errors
export function add(a: number, b: number) {
  return a + b;
}

export function multiply(x: number, y: number) {
  return x * y;
}

const sum = add(1, 2);
const product = multiply(3, 4);

console.log(sum, product);
